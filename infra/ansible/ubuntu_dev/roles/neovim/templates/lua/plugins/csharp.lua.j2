return {
  {
    "seblyng/roslyn.nvim",
    ft = "cs",
    opts = {
      filewatching = "auto",
      broad_search = true,
    },
    config = function(_, opts)
      require("roslyn").setup(opts)
    end,
  },
}
